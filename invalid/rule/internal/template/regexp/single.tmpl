if i.{{ .field_name }} == {{ .rule }} {
    {{- if len_gt_0 .message }}
    err = {{ .message }}
    {{- else }}
    err = errors.New(`invalid<Regexp>: {{ .struct_name }}.{{ .field_name }} must conform to the regex<{{ .regex_value }}>`)
    {{- end }}
    return
}
